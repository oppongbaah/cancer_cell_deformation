[metadata]
long_description = file: README.md
long_description_content_type = text/markdown

[tool:pytest]
testpaths = tests
addopts = -v --tb=short

[tool:coverage:run]
source = celldform
omit = tests/*

[tool:ruff]
line-length = 100
select = ["E", "F", "W", "I"]
